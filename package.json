{
  "name": "openstudio",
  "version": "0.2.0",
  "description": "Live radio studio in your browser. No accounts. No cloud.",
  "type": "module",
  "scripts": {
    "start": "node server/server.js",
    "dev": "node --watch server/server.js",
    "test": "node server/test-signaling.js && node server/test-rooms.js",
    "install": "cd server && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "MIT",
  "devDependencies": {
    "playwright": "^1.56.1"
  }
}
