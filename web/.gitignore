# Node.js (if we add bundler later)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build outputs (if we add bundler later)
dist/
build/
*.tgz

# Temporary files
tmp/
temp/
*.tmp

# Testing
coverage/
.nyc_output/
